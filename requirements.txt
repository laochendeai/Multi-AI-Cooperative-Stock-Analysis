# Core dependencies - required for basic functionality
gradio>=4.32.0
openai>=1.0.0
aiohttp>=3.8.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0
typing-extensions>=4.5.0
rich>=13.0.0

# Stock data and analysis
akshare>=1.12.0
yfinance>=0.2.0
stockstats>=0.5.0

# AI/ML and vector storage
chromadb>=0.4.0
sentence-transformers>=2.2.0
langchain>=0.1.0
langchain-openai>=0.1.0

# Additional dependencies for enhanced features
psutil>=5.9.0  # For system performance monitoring
sqlite3  # Built-in, but listed for clarity
pathlib  # Built-in, but listed for clarity
hashlib  # Built-in, but listed for clarity
base64  # Built-in, but listed for clarity
json  # Built-in, but listed for clarity
re  # Built-in, but listed for clarity
time  # Built-in, but listed for clarity
datetime  # Built-in, but listed for clarity
logging  # Built-in, but listed for clarity
asyncio  # Built-in, but listed for clarity
abc  # Built-in, but listed for clarity
dataclasses  # Built-in, but listed for clarity
pickle  # Built-in, but listed for clarity
subprocess  # Built-in, but listed for clarity
sys  # Built-in, but listed for clarity
os  # Built-in, but listed for clarity

# Optional dependencies for specific providers
# Uncomment as needed based on your LLM providers
# google-generativeai>=0.3.0  # For Google Gemini
# groq>=0.4.0  # For Groq API
# moonshot>=0.1.0  # For Moonshot API
# deepseek>=0.1.0  # For DeepSeek API

# Development dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
